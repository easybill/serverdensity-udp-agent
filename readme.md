run the agent:

cargo run --release -- \
            --token=some_token \
            --agent-key=some_key \
            --account-url=easybill.serverdensity.io \
            --serverdensity-endpoint=http://127.0.0.1:1337